@model Class.Models.ContatoModel

@{
    ViewData["Title"] = "Excluir Contato";
}

<div class="text-center">
    <h1 class="display-4 text-danger">Deseja excluir este contato?</h1>

    <h3>@Model.Nome</h3>
    <h5>@Model.Email</h5>
    <h5>@Model.Telefone</h5>

    <div class="mt-4">
        <a class="btn btn-danger" asp-action="Apagar" asp-route-id="@Model.Id">Confirmar Exclusão</a>
        <a class="btn btn-secondary" asp-action="Index">Cancelar</a>
    </div>
</div>
